name: hello-word
on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Diga oi
        run: echo "Olá, ${{ github.actor }}! Evento: ${{ github.event_name }}"
